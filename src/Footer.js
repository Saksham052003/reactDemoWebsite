import React from "react";

const Footer = () =>{
    return(
        <section className="footer bg-dark text-white">
            <p className="lead container">Contat us at Instagram</p>
            <p className="lead container">You can react via email too</p>
        </section>
    );
};

export default Footer;